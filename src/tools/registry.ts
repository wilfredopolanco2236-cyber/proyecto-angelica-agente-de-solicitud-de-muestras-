export const TOOL_REGISTRY = [
  'user.get_context',
  'catalog.search',
  'client.search',
  'draft.create',
  'draft.add_item',
  'draft.show',
  'pdf.generate_draft',
  'pdf.validate_signed',
  'report.generate',
  'audit.log'
] as const;
